apiVersion: v1
kind: Service
metadata:
  name: prescription-service
  namespace: docrecords
spec:
  selector:
    app: prescription-service
  ports:
    - protocol: TCP
      port: 5004
      targetPort: 5004
  type: ClusterIP
