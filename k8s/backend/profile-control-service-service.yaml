apiVersion: v1
kind: Service
metadata:
  name: profile-control-service
  namespace: docrecords
spec:
  selector:
    app: profile-control-service
  ports:
    - protocol: TCP
      port: 5008
      targetPort: 5008
  type: ClusterIP
