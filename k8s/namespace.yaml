apiVersion: v1           # The Kubernetes API version for this object type
kind: Namespace          # What type of object we're creating
metadata:
  name: docrecords       # The name of our namespace — we'll use this everywhere
